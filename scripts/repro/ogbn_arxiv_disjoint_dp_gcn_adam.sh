#!/usr/bin/env bash
# Placeholder: intended command for ogbn-arxiv disjoint DP-GCN with Adam.
# Does not run training; only echoes. Replace with real invocation when implemented.

set -e
echo "Intended command (dry run):"
echo "  python -m dpgnn_repro.cli --config configs/ogbn_arxiv_disjoint_dp_gcn_adam.json --workdir outputs/ogbn_arxiv_disjoint_dp_gcn_adam"
echo ""
echo "Config ogbn_arxiv_disjoint_dp_gcn_adam.json to be added when disjoint mode is defined."
